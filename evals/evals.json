{
  "version": 2,
  "fixtures": [
    {"id": "synthetic-agent-skill", "presets": ["resume", "technical"]},
    {"id": "software", "presets": ["resume", "technical"]},
    {"id": "sparse", "presets": ["resume", "technical"]}
  ],
  "assertions": [
    "supported baseline facts are not lost",
    "unsupported material claims equal zero",
    "controlled preset runs share one frozen revision and testimony fixture",
    "blind quality comparison and downstream resume evaluation keep independent status",
    "presets differ by sources, facts, questions, and emphasis",
    "depth compares sources, facts, questions, and rendered semantic coverage rather than word count",
    "enabled applicable material modules retain Review dispositions and final render destinations"
  ]
}
