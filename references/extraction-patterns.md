# Extraction Patterns v2

Use [module-registry.md](module-registry.md) to select modules, then load matching module and project-type guidance. Project types identify high-signal sources; module depth defines inspection breadth. A type file is not an output template.
